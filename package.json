{
  "name": "erp-codex",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && rm -rf dist && mkdir -p dist && cp -a frontend/dist/. dist/",
    "preview": "npm --prefix frontend run preview"
  },
  "dependencies": {
    "nodemailer": "^6.9.11"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1"
  }
}
