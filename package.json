{
  "name": "erp-codex",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:vercel": "npm --prefix frontend run build && node build-vercel.js",
    "preview": "npm --prefix frontend run preview",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "dependencies": {
    "google-auth-library": "^9.3.0",
    "googleapis": "^126.0.0",
    "nodemailer": "^6.9.11"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1"
  }
}
